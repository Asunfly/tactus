## 需求总结与预估方案

下面基于你当前已明确的前提来整理：

- **已有侧边插件**
- **插件已支持接入 HTTP 的 MCP**
- 当前缺的是：
  1. **本地 MCP 的通信与远程调用**
  2. **如何把本地 MCP（如 Chrome DevTools / Playwright）转换成 HTTP 可调用能力**
  3. **插件侧如何和这些能力通信**
  4. **危险操作确认机制**
- 目标体验：
  - **右侧插件内对话**
  - **左侧网页自动化操作**
  - Agent 通过 **MCP / tools** 调用能力完成读取、填充、点击、提取等操作

---

# 一、项目目标

## 1. 总体目标

在现有浏览器侧边插件基础上，建立一套 **“对话驱动的网页自动化能力链路”**：

- 用户在插件侧边栏输入自然语言指令
- 插件将请求发给 Agent / MCP 调用层
- 调用层统一通过 **HTTP MCP** 访问能力
- 能力后端驱动：
  - 当前浏览器页面操作
  - 本地浏览器自动化
  - 页面内容读取
  - 表单填写
  - 按钮点击
  - 页面等待与校验
- 对高风险操作提供确认拦截

---

## 2. 核心使用场景

### 场景 A：页面信息提取
- 提取当前页面文本、表格、表单字段、关键信息
- 例如：
  - “帮我提取这页订单列表”
  - “识别当前表单有哪些必填项”

### 场景 B：辅助填写
- 自动填充表单，但不提交
- 例如：
  - “帮我把这个表单先填完，别提交”

### 场景 C：半自动流程执行
- 读取页面状态并逐步点击、切换、填值
- 例如：
  - “帮我完成新建客户流程，最后一步提交前先确认”

### 场景 D：受控自动化
- 用户发起操作后，Agent 连续调用 MCP 工具执行多步流程
- 支持：
  - 等待页面变化
  - 验证结果
  - 遇到危险动作暂停确认

---

# 二、范围定义

## 1. 本期范围

优先支持以下能力：

### 页面读取类
- 获取当前页面 URL、标题
- 获取可见文本
- 获取页面可交互元素
- 获取表单字段清单
- 获取元素基本属性

### 页面操作类
- 填写输入框
- 选择下拉框
- 点击按钮 / 链接
- 勾选复选框
- 滚动到元素
- 等待元素出现 / 页面变化

### 安全控制类
- 危险操作识别
- 执行前确认
- 操作日志记录
- 可回溯每一步执行过程

---

## 2. 暂不优先范围

第一阶段不建议优先做：

- 全网任意站点的完全通用自动化
- 复杂 iframe / shadow DOM 深度支持
- 图像视觉级智能定位
- 跨浏览器多实例同步自动化
- 桌面级 computer use
- 自动绕过验证码、风控、登录校验

---

# 三、当前已有基础

## 已有能力
- 浏览器侧边插件已存在
- 插件已能接入 **HTTP MCP**
- 具备右侧聊天入口

## 当前缺口
- 本地 MCP 与插件链路未打通
- 本地 MCP 无统一 HTTP 暴露层
- 本地工具（如 Chrome DevTools、Playwright）不能直接被插件通过 HTTP 调用
- 危险动作确认策略未明确
- 缺少统一调用协议和执行编排

---

# 四、核心问题拆解

---

## 1. 本地 MCP 如何与插件通信？

### 问题本质
插件只能稳定接入 **HTTP MCP**，但很多浏览器自动化能力是本地进程能力，不是天然 HTTP 服务。

### 需要解决
需要一个 **本地桥接层（Local MCP Gateway / Adapter）**，把本地工具包装成 HTTP 可访问的 MCP 服务。

---

## 2. 如何把本地 MCP / 本地自动化能力暴露为 HTTP？

### 目标
把下面这些能力转换成统一的 HTTP MCP 接口：

- Chrome DevTools 能力
- Playwright 能力
- 本地浏览器会话控制
- 页面元素查询与操作能力

### 本质方案
不是让插件直接连本地工具，而是：

```text
插件 -> HTTP MCP -> 本地桥接服务 -> 本地自动化引擎
```

---

## 3. 本地工具选型怎么定？

### 候选能力源
| 能力源                       | 优点                         | 缺点                               |
| ---------------------------- | ---------------------------- | ---------------------------------- |
| **Chrome DevTools Protocol** | 贴近当前浏览器、读页面信息强 | 操作封装复杂、实现成本高           |
| **Playwright**               | 自动化成熟、填表点击等待强   | 和当前插件页面会话打通需要额外设计 |
| **插件 content script**      | 最贴近当前活动页、实现直接   | 能力边界较窄，复杂自动化弱         |
| **混合方案**                 | 兼顾体验与稳定性             | 架构更复杂                         |

### 推荐结论
**优先采用混合方案**：

- **轻操作 / 当前页读取**：优先走 **插件 content script**
- **稳定自动化 / 复杂流程**：优先走 **Playwright**
- **DevTools**：作为补充能力，不作为第一期唯一执行底座

---

# 五、推荐整体架构

## 1. 逻辑架构

```text
用户
  ↓
侧边插件 Chat UI
  ↓
插件内 Agent / 会话层
  ↓
HTTP MCP 调用层
  ↓
本地 MCP Gateway（新增）
  ├─ Content Script Adapter
  ├─ Playwright Adapter
  └─ Chrome DevTools Adapter
  ↓
页面执行 / 浏览器控制
```

---

## 2. 各模块职责

### A. 侧边插件
负责：
- 用户对话
- 展示执行计划和日志
- 发起 MCP 调用
- 处理危险操作确认
- 展示执行结果

### B. HTTP MCP 层
负责：
- 工具发现
- 请求转发
- 调用统一协议
- 返回标准化结果

### C. 本地 MCP Gateway
这是新增核心组件，负责：
- 将本地能力包装为 HTTP MCP
- 路由不同类型工具调用
- 管理本地连接、会话、错误处理
- 将插件侧请求转成本地执行任务

### D. 执行适配器
#### Content Script Adapter
- 获取当前标签页 DOM
- 提取页面文本
- 查找元素
- 执行简单点击/填充

#### Playwright Adapter
- 处理更稳的自动化流程
- 负责点击、等待、表单、提取、截图等稳定动作

#### Chrome DevTools Adapter
- 负责补充调试能力
- 获取更底层的页面状态
- 可作为高级页面洞察能力

---

# 六、推荐能力分层

## 1. L1：插件当前页能力
通过 content script 暴露，适合：
- 读取页面
- 获取表单
- 轻量填充
- 简单点击

### 典型工具
- `get_page_info`
- `get_visible_text`
- `list_form_fields`
- `list_interactive_elements`
- `fill_field`
- `click_element`

---

## 2. L2：稳定自动化能力
通过 Playwright 暴露，适合：
- 多步骤自动化
- 强等待与验证
- 稳定选择器控制
- 截图与回放

### 典型工具
- `navigate`
- `locator_click`
- `locator_fill`
- `locator_select`
- `wait_for_element`
- `extract_table`
- `take_screenshot`

---

## 3. L3：调试与高级页面信息能力
通过 Chrome DevTools / CDP 暴露，适合：
- 获取更底层页面状态
- 调试和复杂页面诊断
- 网络、DOM、性能、事件补充信息

### 典型工具
- `get_dom_snapshot`
- `inspect_node`
- `get_console_logs`
- `get_network_requests`

---

# 七、通信方案

## 1. 插件到 MCP 的通信

### 当前已具备
- 插件可调用 HTTP MCP

### 继续建议
统一 HTTP 接口格式，至少具备：

- 工具列表查询
- 工具调用
- 结果回执
- 错误信息
- 风险标记

---

## 2. 本地能力转换为 HTTP 的方式

## 推荐方案：新增一个本地服务 `local-mcp-gateway`

### 作用
- 在本机启动一个服务，例如：
  - `http://127.0.0.1:xxxx`
- 对外暴露 HTTP MCP
- 内部连接：
  - Playwright
  - CDP
  - 插件 content script 通道

### 优点
- 插件只认 HTTP，不需要关心本地进程细节
- 本地工具都可以通过适配器接进来
- 后续扩展新工具也方便

---

## 3. content script 如何进入统一链路

这里有两种方式：

### 方式 A：插件内部保留 content script 调用，不走本地 gateway
优点：
- 简单直接
- 当前页能力调用快

缺点：
- 链路不统一
- 与 Playwright / DevTools 的调用模型不同

### 方式 B：通过插件把当前页能力也包装成一个 MCP 工具源
优点：
- 统一抽象
- Agent 看起来只是在调工具

缺点：
- 实现稍复杂

### 推荐
**第一期可采用混合策略**：
- content script 走插件内直连
- 本地 Playwright / DevTools 走 local-mcp-gateway
- 对 Agent 层暴露成统一工具描述即可

---

# 八、危险操作确认方案

这是必须单独设计的。

## 1. 风险分级

### 低风险
- 读取文本
- 提取字段
- 定位元素
- 截图
- 填写但不提交

### 中风险
- 导航跳转
- 点击普通按钮
- 展开/切换页面区域
- 覆盖已有字段

### 高风险
- 提交表单
- 删除内容
- 发送消息
- 支付
- 下单
- 发起不可逆动作

---

## 2. 确认机制

### 低风险
可自动执行

### 中风险
可配置：
- 默认执行
- 或首次提醒

### 高风险
必须二次确认  
插件侧应弹出确认卡片，例如：

> 即将执行高风险操作：点击“提交申请”  
> 预期后果：将正式提交当前表单  
> 是否继续？

按钮：
- `继续执行`
- `取消`
- `以后对该站点同类操作默认询问`

---

## 3. 计划预览
对于多步流程，建议在真正执行前先展示计划：

1. 读取表单字段  
2. 填入申请人信息  
3. 填入费用内容  
4. 点击提交  

然后提示用户确认是否执行第 4 步。

---

## 4. 执行日志
所有动作需要记录：

- 时间
- 工具名
- 参数摘要
- 是否成功
- 是否需确认
- 页面上下文

---

# 九、推荐 MVP 实施方案

## 第一阶段目标
先把“当前页对话式自动化”跑通。

### MVP 功能
- 读取当前页信息
- 识别表单字段
- 填写表单
- 点击普通按钮
- 对提交类操作做确认
- 展示执行日志
- 统一通过 HTTP MCP / 或 Agent 工具层调用

---

## MVP 推荐技术组合

### 页面侧
- 继续使用插件 side panel
- content script 负责当前页读取与轻操作

### 本地桥接侧
- 新增 `local-mcp-gateway`
- 将 Playwright / CDP 包装成 HTTP MCP 服务

### 调用侧
- 插件统一消费 HTTP MCP
- Agent 不关心底层是 content script、Playwright 还是 CDP

### 风险控制
- 高风险动作统一拦截确认

---

# 十、工具能力建议清单

## 第一批建议接入的工具

### 页面信息
- `get_page_info`
- `get_visible_text`
- `get_form_fields`
- `get_interactive_elements`

### 页面操作
- `fill_field`
- `click_element`
- `select_option`
- `scroll_to_element`
- `wait_for_element`

### 自动化增强
- `take_screenshot`
- `extract_table`
- `get_element_value`

### 风险控制
- `classify_action_risk`
- `request_user_confirmation`

---

# 十一、预估实现方案

## 方案 1：轻量混合方案（推荐优先）
### 设计
- 当前页操作：插件 + content script
- 复杂自动化：local-mcp-gateway + Playwright
- 调试补充：后续接入 CDP

### 优点
- 利用现有插件基础最快
- 当前页操作最顺手
- Playwright 补足稳定自动化
- 风险最小，适合先跑通

### 缺点
- 架构上是混合链路
- 统一抽象需要稍做设计

### 适合度
**最高**

---

## 方案 2：全部通过本地 HTTP MCP Gateway 统一收口
### 设计
- content script、Playwright、CDP 都挂到 gateway 下
- 插件只和 gateway 说话

### 优点
- 架构最统一
- 工具模型最清晰
- 后续扩展最好

### 缺点
- 第一版实现成本更高
- 当前页和插件消息桥接更复杂

### 适合度
**中期目标**

---

## 方案 3：纯插件内能力，不引入本地 gateway
### 设计
- 全部依赖插件和 content script

### 优点
- 实现最快

### 缺点
- 自动化能力很快触顶
- 对复杂流程不稳
- Playwright / CDP 无法很好接入

### 适合度
**不建议作为长期方案**

---

# 十二、里程碑建议

## 里程碑 1：当前页基础自动化打通
- 插件对话驱动 content script
- 支持读取、填值、点击
- 支持高风险确认

## 里程碑 2：本地 HTTP MCP Gateway 建立
- 暴露标准 HTTP MCP 服务
- 接入 Playwright Adapter
- 插件可远程调用本地自动化能力

## 里程碑 3：工具统一编排
- 将 content script 和 Playwright 工具统一给 Agent
- 标准化返回结构
- 增加日志、回执、错误恢复

## 里程碑 4：高级能力
- 接入 Chrome DevTools / CDP
- 增加截图、表格提取、页面诊断
- 优化多步流程计划与确认

---

# 十三、主要风险

## 技术风险
- 当前活动页和 Playwright 会话的上下文不一致
- 不同网页选择器稳定性差
- 动态页面等待和回执验证复杂
- 插件与本地服务的权限与通信稳定性

## 产品风险
- 高风险动作误触发
- 用户不理解 agent 即将执行什么
- 多步自动化过程缺乏透明度导致不信任

## 安全风险
- 本地服务暴露 HTTP 后的权限控制
- 敏感页面信息采集与传输
- 高权限网页操作的误用

---

# 十四、建议的最终需求描述

## 产品需求一句话版
在现有浏览器侧边插件中，构建一个支持自然语言交互的网页自动化助手。插件通过 HTTP MCP 调用本地桥接服务，将 content script、Playwright、Chrome DevTools 等本地能力统一封装为可调用工具，实现对当前网页的读取、填充、点击、等待、提取等自动化操作，并对危险操作进行确认拦截和日志记录。

---

## 功能需求版
### 用户可以：
- 在插件右侧对话框中输入自动化指令
- 让 Agent 读取当前网页内容和表单结构
- 自动填充、点击和执行基础流程
- 在提交、删除、发送等危险操作前收到确认提示
- 查看执行步骤与结果

### 系统需要：
- 支持插件调用 HTTP MCP
- 新增本地 MCP Gateway，将本地自动化能力转为 HTTP 可访问
- 统一封装 content script、Playwright、CDP 等能力
- 提供动作分级、确认机制和操作日志
- 支持逐步扩展自动化能力

---

# 十五、建议结论

## 推荐落地路线
### 短期
采用 **混合方案**：
- 插件 + content script 解决当前页读取和轻操作
- 本地 `local-mcp-gateway` 包装 Playwright 为 HTTP MCP
- 高风险动作做统一确认

### 中期
将 content script、Playwright、CDP 能力逐步统一到一个标准工具模型里

### 长期
形成真正统一的“浏览器自动化工具总线”，让 Agent 面向统一工具接口工作

---

## 最后一句总结

### 你现在这个项目的关键，不再是“找什么 skill”
而是：

> **建立一个本地能力到 HTTP MCP 的桥接层，并用统一工具模型把插件侧对话、网页操作和危险动作确认串起来。**

---

如果你愿意，我下一条可以继续给你两版内容中的任意一种：

1. **PRD/技术方案文档版**
2. **按模块拆解的开发任务清单版**